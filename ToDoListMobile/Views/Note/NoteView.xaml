<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:ToDoListMobile.ViewModels.Note;assembly=ToDoListMobile"
             x:Class="ToDoListMobile.Views.Note.NoteView"
             Title="New note">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Header" />
            <Entry x:Name="Header"
                   Text="{Binding Header}"/>
            <Label Text="Date Added" />
            <DatePicker Date="{Binding DateAdded}" />
            <Label Text="Text" />
            <Editor Text="{Binding Text}" />
            <Button Text="Save" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>